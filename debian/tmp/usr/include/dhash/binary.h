#ifndef _DHASH_BINARY_H
#define _DHASH_BINARY_H

#include <stdio.h>

void *binary2string(const void *p, size_t pn, char *str, size_t strn);
void print_binary(const void *p, size_t n);

#endif /* _DHASH_BINARY_H */
